import React from 'react';

function RecommendedProducts(props) {

    const { categoryId } = props

    const _getRecommendedProducts = async (categoryId) => {

    }

    return (
        <div>

        </div>
    );
}

export default RecommendedProducts;